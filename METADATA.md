The data in the SubsetData folder within this repository contains a subset of the Danish Gigaword Corpus. This way one can run the scripts on a subset of the data and get an insight into how the word2vec model was trained on the entire corpus. If you want to want the preprocessing script (link) then you have to specify the path to the subset data text files. Once you have preprocessed the data files, you can run the Python script and specify a path to the preprocessed data files and train a model on this preprocessed data. 
The Danish Gigaword Corpus (DAGW) consists of 15 sections and each section represents the source from which the data comes from. The subset of the data consists of three files from each section. 



￼

If you only wish to run the Shiny application you only have to run the prerequisites file in order to install the required packages and get the needed variables in your R environment in order to run the app successfully. 

